@using XMS.Integration.OneC.Ut.Features.Catalog_СтатьиДвиженияДенежныхСредств_Feature
@using XMS.Modules.CostModule.Abstractions
@using XMS.Modules.CostModule.Domain

<MudToggleIconButton @bind-Toggled="@_open"
					 Icon="@Icons.Material.Filled.Add"
					 Color="@Color.Primary"
					 ToggledIcon="@Icons.Material.Filled.FullscreenExit"
					 ToggledColor="@Color.Secondary"
					 Size="Size.Small" />
@if (_open)
{
	<MudDrawer @bind-Open="@_open"
			   Anchor="Anchor.End"
			   ClipMode="DrawerClipMode.Always"
			   Elevation="1"
			   Variant="@DrawerVariant.Temporary"
			   OverlayAutoClose="false"
			   Width="800px"
			   Class="me-5">
		<MudDrawerHeader>
			<MudText Typo="Typo.h6">Статьи ДДС (1С Бух)</MudText>
		</MudDrawerHeader>
		<MudPaper Style="max-height: calc(100vh - 200px); overflow-y: auto;">
			<MudTreeView Items="@(CatalogUtItemsTree)" T="Catalog_СтатьиДвиженияДенежныхСредств" Dense="true">
				<ItemTemplate Context="item">
					<MudTreeViewItem Expanded="@item.Expanded"
									 ExpandedChanged="@((expanded) => ExpandedChanged(item, expanded))"
									 Items="@item.Children"
									 Value="@item.Value">
						<Content>
							<MudTreeViewItemToggleButton Expanded="@item.Expanded"
														 ExpandedChanged="@((expanded) => ExpandedChanged(item, expanded))"
														 Visible="@item.HasChildren" />
							<MudIcon Icon="@GetIcon(item)" Class="@(item.Value?.DeletionMark == true ? "mr-2 text-secondary" : "mr-2")" />

							<MudStack Row="true" Class="d-flex justify-space-between flex-grow-1 gap-4">
								@if (item.Value?.IsFolder == false)
								{
									<MudCheckBox T="bool"
												 Value="@(SelectedCatalogUtItemIds.Contains(item.Value.Ref_Key))"
												 ValueChanged="@((bool isChecked) => CheckBoxChanged(isChecked, item.Value.Ref_Key))">
										<MudText Typo="Typo.body1">@item.Value?.Description</MudText>
									</MudCheckBox>
								}
								else
								{
									<MudText Typo="Typo.body1">@item.Value?.Description</MudText>
								}
								<MudText>@item.Value?.Code</MudText>
							</MudStack>
						</Content>
					</MudTreeViewItem>
				</ItemTemplate>
			</MudTreeView>
		</MudPaper>
		@if (_isProcessing)
		{
			<MudProgressLinear Color="Color.Primary" Indeterminate="true" />
		}
		<MudStack Row="true" Class="mt-2" Justify="Justify.SpaceAround">
			<MudButton OnClick="@UpdateCostCatalog" Color="Color.Primary" Variant="Variant.Filled">Сохранить</MudButton>
			<MudButton OnClick="@ToggleOpen" Color="Color.Secondary" Variant="Variant.Filled">Закрыть</MudButton>
		</MudStack>
	</MudDrawer>
}

@code {
	[Parameter] public Guid CostCategoryItemId { get; set; } = default!;
	[Parameter] public IReadOnlyList<TreeItemData<Catalog_СтатьиДвиженияДенежныхСредств>> CatalogUtItemsTree { get; set; } = default!;
	[Parameter] public HashSet<Guid> SelectedCatalogUtItemIds { get; set; } = default!;
	[Parameter] public EventCallback<List<CostCatalogUt>> OnUpdateCostCatalog { get; set; }

	private HashSet<Guid> _expandedCatalogUtItemIds = [];

	private bool _open;
	private bool _isProcessing;

	private void ToggleOpen() => _open = !_open;

	private async Task ExpandedChanged(ITreeItemData<Catalog_СтатьиДвиженияДенежныхСредств?> node, bool expanded)
	{
		node.Expanded = expanded;

		if (node.Value is Catalog_СтатьиДвиженияДенежныхСредств catalogUtItem)

		{
			if (expanded)
				_expandedCatalogUtItemIds.Add(catalogUtItem.Ref_Key);
			else
				_expandedCatalogUtItemIds.Remove(catalogUtItem.Ref_Key);
		}
	}

	private static string GetIcon(ITreeItemData<Catalog_СтатьиДвиженияДенежныхСредств> item)
	{
		if (item.HasChildren)
			return item.Expanded ? Icons.Material.Filled.FolderOpen : Icons.Material.Filled.Folder;

		return Icons.Material.Filled.Description;
	}

	private void CheckBoxChanged(bool isChecked, Guid itemId)
	{
		if (isChecked)
		{
			SelectedCatalogUtItemIds.Add(itemId);
		}
		else
		{
			SelectedCatalogUtItemIds.Remove(itemId);
		}
	}

	private async Task UpdateCostCatalog()
	{
		_isProcessing = true;
		var newCostCatalogUtList = SelectedCatalogUtItemIds
			.Select(refKey => new CostCatalogUt { CatalogUtRefKey = refKey, CostCategoryItemId = CostCategoryItemId })
			.ToList();

		await OnUpdateCostCatalog.InvokeAsync(newCostCatalogUtList);
		
		_isProcessing = false;
		ToggleOpen();
	}
}
